stages:
  - Test

jest-tests:
  stage: Test
  image: node:20-slim
  before_script:
    - cd ./apps/orders
    - npm install
  script:
    - npm run test:coverage
  artifacts:
    when: always
    paths:
      - coverage/
